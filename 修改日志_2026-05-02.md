# 晨心AI写作助手 - 代码修改日志

日期: 2026-05-02
执行者: Hermes Agent (mimo-v2.5-pro)

---

## 一、后端 Bug 修复（6项）

### BUG-1: VolumeOutlineCreate schema 字段名不匹配 ✅
- 文件: backend/services/volume_outline_service.py:52-54
- 问题: schema 使用 name/description，service 读取 volume_name/summary
- 修复: 兼容两种字段名，优先 volume_name/summary，回退到 name/description

### BUG-2: scene_point INSERT 缺少 scene_type 字段 ✅
- 文件: backend/routers/outlines.py:173-178
- 问题: add_scene_point 端点 INSERT 语句未包含 scene_type
- 修复: INSERT 语句加入 scene_type 列

### BUG-3: replace_scenes INSERT 缺少 scene_type 字段 ✅
- 文件: backend/routers/outlines.py:194-200
- 问题: 批量替换场景时 INSERT 语句未包含 scene_type
- 修复: INSERT 语句加入 scene_type 列

### BUG-4: CHAPTER_FIELDS 白名单遗漏 ✅
- 文件: backend/routers/_common.py:23
- 问题: volume_label 和 arc_label 不在白名单中，UPDATE chapters 时被过滤
- 修复: 白名单加入 volume_label, arc_label

### BUG-5: style_analysis prompt 模板转义错误 ✅
- 文件: backend/utils/prompt_manager.py:19
- 问题: {context} 后面有多余的反斜杠，导致字面量 \n 而非换行
- 修复: 重写整个文件，确保所有模板使用正确的转义

### BUG-6: ON CONFLICT + lastrowid 兼容性问题 ✅
- 文件: backend/services/settings_library_service.py:54-56
- 问题: ON CONFLICT DO UPDATE 时 lastrowid 可能返回 0
- 修复: 当 lastrowid 为 0 时，回退到按唯一键 (project_id, category, name) 查询

---

## 二、前端 Bug 修复（5项）

### BUG-7: SidebarVersions 版本列表不随章节切换刷新 ✅
- 文件: frontend/src/components/WriteWizard/SidebarVersions.jsx:9-16
- 问题: loadedRef 模式只加载一次，切换章节不刷新
- 修复: 移除 loadedRef，useEffect 依赖改为 [projectId, nextChapter]

### BUG-8: SidebarChat 同样只加载一次 ✅
- 文件: frontend/src/components/WriteWizard/SidebarChat.jsx:10-17
- 修复: 移除 loadedRef，useEffect 依赖改为 [projectId]

### BUG-9: SidebarOutlineContext 色彩不一致 ✅
- 文件: frontend/src/components/WriteWizard/SidebarOutlineContext.jsx
- 修复: text-gray-300 → text-ink-300, hover:text-emerald-500 → hover:text-jade-500（2处）

### BUG-10: SidebarOutlineContext 伏笔字段不存在 ✅
- 文件: frontend/src/components/WriteWizard/SidebarOutlineContext.jsx:139
- 问题: 引用 f.keyword 但数据库无此字段
- 修复: 改为 f.description + f.importance

### BUG-11: SidebarOutlineContext 时间线字段全部错误 ✅
- 文件: frontend/src/components/WriteWizard/SidebarOutlineContext.jsx:155-163
- 问题: 引用 t.event_description, t.in_chapter_time, t.characters_involved（均不存在）
- 修复: 改为 t.chapter_number, t.story_time_description, t.summary

---

## 三、大纲页面简化

### 3.1 总纲编辑器简化 ✅
- 文件: frontend/src/components/OverallOutlineEditor.jsx（完全重写）
- 改动: 删除结构化 JSON 卡片展示，改为一个大 textarea（h-96）
- 保留: AI 生成按钮、保存按钮
- AI 生成后自动将 JSON 格式化为可读文本显示
- 修复 BUG: 保存格式导致内容层层包裹的问题（{content: text} 循环）

### 3.2 章纲编辑器简化 ✅
- 文件: frontend/src/components/ChapterOutlinePanel.jsx（大幅简化）
- 改动:
  - 列表行只显示: 章号、标题、情节描述(truncated)、章末钩子
  - 展开编辑区只保留: 标题(可选)、情节描述(textarea)、章末钩子(input)
  - 完全删除 scene_points 相关 UI
  - 保存时只保存 title, core_objective, hooks

### 3.3 续写侧栏章纲显示简化 ✅
- 文件: frontend/src/components/WriteWizard/SidebarOutlineContext.jsx
- 改动: 只显示标题、情节描述(core_objective)、章末钩子(hooks)
- 删除: 场景要点(scenes)、emotional_arc、rhythm_type、chapter_opening 显示

### 3.4 后端章纲生成 Prompt 简化 ✅
- 文件: backend/services/outline_service.py
- 改动:
  - 单章生成: 输出格式简化为 title + core_objective(情节描述) + hooks(章末钩子)
  - 批量生成: 同样简化输出格式
  - 保存逻辑: 去掉 scene_points 保存，只保存 chapter_outlines 三个字段

---

## 四、大文件上传处理

### 4.1 新建大文件处理服务 ✅
- 文件: backend/services/large_import_service.py（新建）
- 功能:
  - group_chapters_into_chunks(): 按5万字分组章节
  - process_chunk_outlines(): 处理单个chunk，AI生成章纲+提取元数据
  - large_import_and_process(): 主流程，分块处理所有已导入章节
- 支持最大30万字一次性处理

### 4.2 新增大文件处理端点 ✅
- 文件: backend/routers/import_chapters.py（新增端点）
- 端点: POST /api/projects/{id}/import/large-process
- 功能: 调用 large_import_and_process 执行分块处理

### 4.3 前端 API 接口 ✅
- 文件: frontend/src/api/client.js（新增函数）
- 新增: largeProcessImport(pid)，timeout 600秒

### 4.4 前端导入面板集成 ✅
- 文件: frontend/src/components/ImportPanel.jsx
- 改动:
  - 导入 largeProcessImport
  - 根据总字数（>5万字）自动选择大文件处理流程
  - 大文件处理结果展示（分块数、章纲数、角色数等）

---

## 五、代码审查修复（第二轮）

### BUG-12: OverallOutlineEditor 保存格式层层包裹 ✅
- 问题: {content: text} 保存后加载，formatOutlineToText 将 content 当未知字段处理
- 修复: formatOutlineToText 优先检测 {content: "..."} 格式，直接返回纯文本

### BUG-13: SidebarVersions handleRestore 缺少 await ✅
- 修复: loadVersions() 前加 await

### BUG-14: ImportPanel 大文件阈值应基于字数而非章数 ✅
- 修复: res.imported > 5 → res.total_words > 50000

---

## 修改文件清单

### 后端（10个文件）
1. backend/services/volume_outline_service.py - 修复字段名不匹配
2. backend/routers/outlines.py - 修复 scene_type 插入（2处）
3. backend/routers/_common.py - 扩展 CHAPTER_FIELDS 白名单
4. backend/utils/prompt_manager.py - 修复转义错误，重写文件
5. backend/services/settings_library_service.py - 修复 lastrowid 问题
6. backend/services/outline_service.py - 简化章纲生成prompt和保存
7. backend/services/large_import_service.py - 新建大文件处理服务
8. backend/routers/import_chapters.py - 新增 large-process 端点
9. backend/services/overall_outline_service.py - 未改动（兼容新格式）
10. backend/models/schemas.py - 未改动

### 前端（7个文件）
1. frontend/src/components/WriteWizard/SidebarVersions.jsx - 修复刷新+await
2. frontend/src/components/WriteWizard/SidebarChat.jsx - 修复加载
3. frontend/src/components/WriteWizard/SidebarOutlineContext.jsx - 修复字段+色彩+简化显示
4. frontend/src/components/OverallOutlineEditor.jsx - 完全重写为纯文本编辑
5. frontend/src/components/ChapterOutlinePanel.jsx - 大幅简化章纲编辑
6. frontend/src/components/ImportPanel.jsx - 集成大文件处理
7. frontend/src/api/client.js - 新增 largeProcessImport

---

## 未提交状态
所有修改均在工作区，未 commit。共修改/新建 17 个文件。
