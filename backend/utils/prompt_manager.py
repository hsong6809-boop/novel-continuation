"""提示词管理器 - 从 settings.json 读取提示词模板，支持变量替换"""
import json
import logging
from pathlib import Path
from config import BASE_DIR
from utils.settings_cache import load_settings

logger = logging.getLogger(__name__)

DEFAULT_TEMPLATES = {
    "continuation": "你是一个专业的网文续写 AI。你的任务是根据「前文」的剧情和文风，续写「本章章纲」所描述的内容。\n{context}\n\n## 写作要求\n1. 严格按「本章章纲」的情节描述写作，不要添加纲要中未提及的新情节、新冲突或新场景\n2. 保持与「前文」一致的文风——前文就是你的范文，模仿其句式、用词、对话节奏、描写密度\n3. 写完章纲描述的内容后自然收尾，不要自行扩展纲要外的情节\n4. 正文直接输出，不要输出章节标题、编号或任何元说明",
    "chapter_outline": "你是一个资深的男频网文架构师。请为第{chapter}章生成章纲。\n{context}\n\n## 生成流程（严格按顺序执行）\n\n### 第一步：定位\n仔细阅读「前文内容」，结合「所属卷大纲」中的阶段划分（phases），判断当前剧情的实际进展：\n- 前文实际写到了卷纲的哪个阶段\n- 该阶段已经完成了哪些情节点，还剩什么没有覆盖\n- 前文是否存在偏离卷纲计划的情况（如有，以实际正文为准）\n\n### 第二步：判断\n根据定位结果，判断第{chapter}章：\n- 应该继续推进当前阶段，还是进入下一阶段\n- 本章需要推进什么具体事件\n- 本章需要向读者传递什么关键信息\n- 本章在变强路径上需要体现什么进展\n\n### 第三步：输出\n基于以上分析，生成本章章纲。\n\n## 字数约束\n本章正文约2000-2500字，请据此控制情节容量。\n\n### core_objective 写法规范（核心）\ncore_objective 是给续写AI的\"路线图\"，不是分镜脚本。只写关键节点，不写具体动作。\n\n❌ 太细（逐动作分解）：\n\"陆沉将妹妹锁在屋内，独自引敌入死巷，以伤换伤击倒三人，被王瘸子用铁尺砸伤肩膀，拼死撞翻王瘸子，用膝盖压住喉咙逼退剩余两人\"\n✅ 合适（关键节点）：\n\"王瘸子带人夜袭，陆沉引敌入死巷苦战，以重伤为代价击退敌人\"\n\n规则：\n- 限3-5个关键点，总字数不超过100字\n- 战斗/对话只写结果和转折，不写过程细节\n- 具体怎么打、怎么说是续写AI的事\n\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "batch_outline": "你是一个资深的男频网文架构师。请为第{ch_start}章到第{ch_end}章批量生成章纲。\n{context}\n\n## 生成流程（严格按顺序执行）\n\n### 第一步：定位\n仔细阅读「前文内容」，结合「所属卷大纲」中的阶段划分（phases），判断当前剧情的实际进展：\n- 前文实际写到了卷纲的哪个阶段\n- 该阶段还剩什么没有覆盖\n- 前文是否存在偏离卷纲计划的情况（如有，以实际正文为准）\n\n### 第二步：规划\n根据定位结果，规划这批章节：\n- 从哪个阶段的哪个事件开始\n- 这批章节整体需要推进哪些事件\n- 节奏如何安排（铺垫→爆发→过渡）\n\n### 第三步：逐章输出\n基于以上分析，逐章生成章纲。\n\n## 字数约束\n每章正文约2000-2500字，请据此控制每章的情节容量。\n\n### core_objective 写法规范（核心）\ncore_objective 是给续写AI的\"路线图\"，不是分镜脚本。只写关键节点，不写具体动作。\n\n❌ 太细（逐动作分解）：\n\"陆沉将妹妹锁在屋内，独自引敌入死巷，以伤换伤击倒三人，被王瘸子用铁尺砸伤肩膀，拼死撞翻王瘸子，用膝盖压住喉咙逼退剩余两人\"\n✅ 合适（关键节点）：\n\"王瘸子带人夜袭，陆沉引敌入死巷苦战，以重伤为代价击退敌人\"\n\n规则：\n- 限3-5个关键点，总字数不超过100字\n- 战斗/对话只写结果和转折，不写过程细节\n- 具体怎么打、怎么说是续写AI的事\n- 每章的 info_delivery 要明确：本章读者会获得什么新信息\n\n## 注意\n- 不要重复已有章纲的内容\n- 章纲之间要有递进关系，不要各自独立\n\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "volume_outline": "你是一个资深的男频网文架构师。请根据已有正文和总纲框架，为这部小说规划下一分卷的大纲。\n{context}\n\n## 核心原则\n1. 已有正文是你的核心输入，总纲是辅助框架——不要偏离总纲的方向，但具体情节从正文中推导\n2. 如果这是第一卷：则根据现有章节能提供的信息向后推导，包括现有章节的情节，并且第一卷的卷纲需要包含已有正文的情节。\n3. 如果不是第一卷：基于上一卷的全部前文展开，确保剧情衔接\n4. 默认情节范围为 100 章（约 20 万字），这是网文一卷的标准篇幅\n5. 必须明确本卷结束时主角和相关事物将进展到什么程度，核心爽点在本卷哪些情节去呈现出来，通过什么情节对比出来。\n6. 规划本卷的阶段划分，每个阶段要有具体的核心冲突和解决方式\n7. 本卷要有明确的变强路径——主角在本卷结束时比开始时强了多少，怎么强的\n\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "overall_outline": "你是一个资深的男频网文架构师。请根据已有正文内容，为这部小说提炼总纲。\n{context}\n\n## 核心原则\n总纲是约束性框架，不是详细规划。你的任务是：\n1. 从已有正文中提取关键信息，而不是凭空编造\n2. 定好起点、核心主线、终点方向，但不框死具体情节细节。终点必须是足够遥远的虚化方向（如「站上武道之巅」），不要编造具体结局——网文的结局在一开始是无法确定的\n3. 总纲只定「方向」，具体的情节由卷纲和章纲来规划\n4. 男频网文的核心主线本质是「变强」——高魔世界观是自身实力变强，低魔/无武世界观是势力和社会地位变强，但都是「向上走」\n5. key_info 必须详尽：世界观的核心设定、力量体系、势力格局、核心矛盾的来龙去脉、主要角色关系网、前期目标、核心卖点——这些都要从正文中抓取出来\n\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "meta_extraction": "你是一个小说分析助手。请从以下章节内容中提取元数据。\n{context}\n\n只提取确实存在的信息，不要编造。如果某项为空，返回空数组或空对象。\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "preprocess": "你是一个专业的小说分析助手。请对以下已导入的小说章节进行全面分析。\n{context}\n\n请直接输出 JSON，不要加 markdown 代码块标记。",
    "chat_system": "你是一个专业的小说创作助手，正在帮助作者进行小说续写。\n你拥有当前项目的完整上下文，包括角色、伏笔、时间线、最近章节内容和章纲。\n请基于这些信息回答作者的问题，给出具体、可操作的建议。\n{context}\n\n请用专业但友好的语气回答，给出具体可操作的建议。\n重要：请用纯文本格式回复，不要使用 markdown 语法（不要用 # 标题、**加粗**、- 列表、```代码块```等）。用自然段落和换行来组织内容。",
    "style_analysis": "你是一个专业的文学风格分析师。请分析以下小说片段的写作风格。\n{context}\n\n请从以下维度进行分析，并给出简洁的总结：\n1. 叙事视角（第一人称/第三人称限制/第三人称全知等）\n2. 语言风格（简洁/华丽/口语化/文言化等）\n3. 描写特点（白描/工笔/意识流等）\n4. 对话风格（简洁/冗长/方言/书面语等）\n5. 节奏特点（快节奏/慢节奏/张弛有度等）\n6. 常用修辞手法\n7. 段落结构特点\n\n请用中文输出，控制在500字以内。",
}


def _load_settings() -> dict:
    return load_settings()


def get_prompt(feature: str) -> str:
    """获取指定功能的提示词模板。优先从 settings.json 读取，找不到则返回默认值。"""
    settings = _load_settings()
    return settings.get("prompts", {}).get(feature, DEFAULT_TEMPLATES.get(feature, ""))


def format_prompt(feature: str, **kwargs) -> str:
    """加载模板并执行变量替换。

    Args:
        feature: 功能标识
        **kwargs: 模板变量，如 chapter=10, context="..."

    Returns:
        格式化后的提示词
    """
    template = get_prompt(feature)
    try:
        return template.format(**kwargs)
    except (KeyError, ValueError, IndexError) as e:
        logger.warning("提示词模板变量缺失: feature=%s error=%s", feature, e)
        return template